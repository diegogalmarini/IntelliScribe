# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent tooling — .claude/ contiene worktrees (copia completa del repo).
# .agents/ SÍ se versiona: contiene skills instalados (find-skills).
.claude/
supabase/.temp/

# Security
# Repo público: cualquier variante de .env queda fuera salvo el ejemplo sin secretos
.env
.env*
!.env.example
temp_*.txt
*.pem
*.key

# Contexto de negocio interno: cifras de usuarios, MRR y notas de
# posicionamiento. El repositorio es publico.
.agent/product-marketing-context.md

# Plantillas de instrucciones para OTROS proyectos (Next.js), no para Diktalo.
# Se ignoran en vez de borrarse: son notas del usuario, no ficheros del repo.
claude_seo.md
claude_web.md
.tmpwork/
