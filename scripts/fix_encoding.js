const fs = require('fs');
const path = 'C:\\Users\\diego\\Diktalo\\utils\\blogData.ts';
// This script is meant to fix the encoding if possible, but since I don't have the original correct text in memory for the whole file,
// I should have used a better tool for the deletion.
// Actually, I can just undo the git commit and try a different deletion method that preserves encoding.
